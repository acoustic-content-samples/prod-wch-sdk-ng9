import { AbstractSitesBackgroundComponent } from './abstract.sites.background.component';

/** Useful imports */
// import { map, takeUntil, distinctUntilChanged } from 'rxjs/operators';

/*
 * @name Sites Background
 * @id 0a92059b-de6b-476d-b291-1638a435d0af
 * @description Use for all backgrounds.
 */
export abstract class TypeSitesBackgroundComponent extends AbstractSitesBackgroundComponent {

    /*
     * TODO add custom fields here. These fields should be those
     * common to all layouts.
     */

    protected constructor() {
        super();
    }

}
