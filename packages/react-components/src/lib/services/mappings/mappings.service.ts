import { LayoutMappingResolver } from '@acoustic-content-sdk/component-api';
import { AbstractLayoutMappingResolver } from '@acoustic-content-sdk/component-utils';

export class LayoutMappingResolverService extends AbstractLayoutMappingResolver
  implements LayoutMappingResolver {
  constructor() {
    super();
  }
}
