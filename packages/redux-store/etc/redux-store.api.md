## API Report File for "@acoustic-content-sdk/redux-store"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { Action } from 'redux';
import { AnyAction } from 'redux';
import { Consumer } from '@acoustic-content-sdk/utils';
import { Epic } from 'redux-observable';
import { EqualsPredicate } from '@acoustic-content-sdk/utils';
import { LoggerService } from '@acoustic-content-sdk/api';
import { Observable } from 'rxjs';
import { OperatorFunction } from 'rxjs';
import { Reducer } from 'redux';
import { Store } from 'redux';
import { UnaryFunction } from 'rxjs';

// @public
export function createIdentifier(idOrModuleId: string | ReduxFeatureModuleId<any, any>, aValue?: string): string;

// @public
export function createReduxFeatureModule<S, FS = any, Input extends Action = AnyAction, Output extends Input = Input, Dependencies = any>(idOrModuleId?: string | ReduxFeatureModuleId<S, FS>, reducer?: Reducer<S>, epic?: Epic<Input, Output, ReduxRootState, Dependencies>, dependencies?: ArrayLike<ReduxFeatureModule<any>>): ReduxFeatureModule<S, FS>;

// @public
export function createReduxMetaModule<FS = any>(dependencies: ArrayLike<ReduxFeatureModule<any>>): ReduxFeatureModule<any, FS>;

// @public
export function createReduxRootStore(aDependencies: any, aPreLoadedState?: ReduxRootState): ReduxRootStore;

// @public
export function featureModuleId<S, FS = any>(id?: string): ReduxFeatureModuleId<S, FS>;

// @public
export function ofInitFeature<A = AnyAction>(idOrModuleId: string | ReduxFeatureModuleId<any, any>): OperatorFunction<A, string>;

// @public
export interface PayloadAction<T> extends Action {
    // Warning: (ae-forgotten-export) The symbol "ACTION_PAYLOAD" needs to be exported by the entry point public_api.d.ts
    //
    // (undocumented)
    [ACTION_PAYLOAD]: T;
}

// @public
export interface ReduxFeatureModule<S, FS = any, Input extends Action = AnyAction, Output extends Input = Input, Dependencies = any> extends ReduxFeatureModuleId<S, FS> {
    dependencies?: ArrayLike<ReduxFeatureModule<any>>;
    epic?: Epic<Input, Output, ReduxRootState, Dependencies>;
    id: string;
    reducer?: Reducer<S>;
}

// @public
export interface ReduxFeatureModuleId<S, FS = any> {
    id: string;
}

// @public
export type ReduxRootState = Record<string, any>;

// @public
export interface ReduxRootStore<S = ReduxRootState, A extends Action = AnyAction> extends Store<S, A> {
    addFeatureModule<RFS = any, FS = any>(aFeature: ReduxFeatureModule<RFS, FS>): ReduxRootStore<FS, A>;
}

// @public
export interface ReduxRootStoreDependencies {
    logSvc: LoggerService;
    rootStore: ReduxRootStore;
}

// @public
export const rxDispatch: <S>(aStore: Store<S>) => Consumer<AnyAction>;

// @public
export const rxSelect: <T, R>(aSelector: UnaryFunction<T, R>, aCmp?: EqualsPredicate<R>) => OperatorFunction<T, R>;

// @public
export function rxSelectFeature<S, FS = any>(idOrModuleId: string | ReduxFeatureModuleId<S, FS>): UnaryFunction<ReduxRootStore, Observable<S>>;

// @public
export function rxStore<S>(aStore: Store<S>): Observable<S>;

// @public
export const selectFeature: <S, FS = any>(idOrModuleId: string | ReduxFeatureModuleId<S, FS>, aDefaultState?: S) => UnaryFunction<ReduxRootState, S>;

// @public
export const selectPayload: <T>(aAction: PayloadAction<T>) => T;

// @public
export const STORE_ID = "@acoustic-content-sdk/redux-store";

// @public
export const VERSION: {
    version: {
        major: string;
        minor: string;
        patch: string;
    };
    build: Date;
};


```
