import { UrlConfig } from '@acoustic-content-sdk/api';
import { EMPTY, Observable } from 'rxjs';

/**
 * Constant for the url config
 */
export const ACOUSTIC_URL_CONFIG: Observable<UrlConfig> = EMPTY;
