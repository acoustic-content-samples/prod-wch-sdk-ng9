import { ReactReduxContext } from 'react-redux';

/**
 * Injection token for the {@link https://react-redux.js.org/|react redux} store
 */
export const ACOUSTIC_CONTEXT_REACT_REDUX = ReactReduxContext;
