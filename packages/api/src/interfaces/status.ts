export enum Status {
  DRAFT = 'draft',
  READY = 'ready',
  RETIRED = 'retired'
}

/**
 * Possible values for draft status
 */
export enum DraftStatus {
  IN_PROGRESS = 'in-progress',
  IN_REVIEW = 'in-review',
  APPROVED = 'approved'
}
